apiVersion: apps/v1
kind: Deployment
metadata:
  name: aether-flow
  namespace: aether-flow
  labels:
    app: aether-flow
    app.kubernetes.io/name: aether-flow
    app.kubernetes.io/part-of: aether
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aether-flow
  template:
    metadata:
      labels:
        app: aether-flow
        app.kubernetes.io/name: aether-flow
    spec:
      serviceAccountName: aether-flow
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: aether-flow
          image: ghcr.io/suplab/aether-flow:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8085
              protocol: TCP
          env:
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: aether-flow-secrets
                  key: postgres-url
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: aether-flow-secrets
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: aether-flow-secrets
                  key: postgres-password
            - name: FLOW_ESCALATION_CRON
              valueFrom:
                configMapKeyRef:
                  name: aether-flow-config
                  key: escalation-cron
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
